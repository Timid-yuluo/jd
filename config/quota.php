<?php

declare(strict_types=1);

return [
    /*
    |--------------------------------------------------------------------------
    | 次卡自动选择路由
    |--------------------------------------------------------------------------
    |
    | 当月配额耗尽但有次卡可用时，默认行为是返回 403 让用户确认选择次卡。
    | 仅以下配置的路由会自动选择最佳次卡（优先专用次卡，其次通用次卡），
    | 无需用户手动确认。
    |
    */
    'credit_auto_select_routes' => [
        // 默认为空：所有路由都需要用户确认选择次卡
        // 如需某路由自动选择次卡，在此添加路由名，例如：
        // 'user.resumes.keywords.extract',
    ],
];
